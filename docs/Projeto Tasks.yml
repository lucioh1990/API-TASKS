opencollection: 1.0.0
info:
  name: Projeto Tasks
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ''
      port: ''
      auth:
        username: ''
        password: ''
      bypassProxy: ''
items:
  - info:
      name: Auth
      type: folder
      seq: 1
    request:
      auth: inherit
    items:
      - info:
          name: POST LOGIN
          type: http
          seq: 2
        http:
          method: POST
          url: http://localhost:3000/login
          body:
            type: json
            data: |-
              {
                "email": "teste12.06@email.com",
                "password": "minhaSenha123"
              }
          auth: inherit
        runtime:
          scripts:
            - type: after-response
              code: |-
                const data = res.getBody();
                bru.setVar("token", data.token);
        settings:
          encodeUrl: true
          timeout: 0
          followRedirects: true
          maxRedirects: 5
      - info:
          name: POST Register
          type: http
          seq: 1
        http:
          method: POST
          url: http://localhost:3000/register
          body:
            type: json
            data: |-
              {
                "email": "teste12.06@email.com",
                "password": "minhaSenha123"
              }
          auth: inherit
        settings:
          encodeUrl: true
          timeout: 0
          followRedirects: true
          maxRedirects: 5
  - info:
      name: Tasks
      type: folder
      seq: 2
    request:
      auth: inherit
    items:
      - info:
          name: GET Task
          type: http
          seq: 5
        http:
          method: GET
          url: http://localhost:3000/tasks
          headers:
            - name: Authorization
              value: Bearer {{token}}
          params:
            - name: Authorization
              value: Bearer {{token}}
              type: query
              disabled: true
          auth: inherit
        settings:
          encodeUrl: true
          timeout: 0
          followRedirects: true
          maxRedirects: 5
      - info:
          name: GET Task ID
          type: http
          seq: 4
        http:
          method: GET
          url: http://localhost:3000/tasks/6
          headers:
            - name: Authorization
              value: Bearer {{token}}
          auth: inherit
        settings:
          encodeUrl: true
          timeout: 0
          followRedirects: true
          maxRedirects: 5
      - info:
          name: POST Criar Task
          type: http
          seq: 3
        http:
          method: POST
          url: http://localhost:3000/tasks
          headers:
            - name: Authorization
              value: Bearer {{token}}
          body:
            type: json
            data: |-
              {
                "title":       "Apresentação teste333333",
                "description": "Sprint S02 do roadmap",
                "status":      "doing",
                "priority":    "high",
                "projectId": 2,
                "categoryId": 1
              }
          auth: inherit
        settings:
          encodeUrl: true
          timeout: 0
          followRedirects: true
          maxRedirects: 5
      - info:
          name: PUT Atualizar Task
          type: http
          seq: 2
        http:
          method: PUT
          url: http://localhost:3000/tasks/6
          headers:
            - name: Authorization
              value: Bearer {{token}}
          body:
            type: json
            data: |-
              {
                "title":       "Apresentação Q1 (atualizado)",
                "description": "Concluído!",
                "status":      "done",
                "priority":    "high"
              }
          auth: inherit
        settings:
          encodeUrl: true
          timeout: 0
          followRedirects: true
          maxRedirects: 5
      - info:
          name: DELETE Deletar Task
          type: http
          seq: 1
        http:
          method: DELETE
          url: http://localhost:3000/tasks/6
          headers:
            - name: Authorization
              value: Bearer {{token}}
          auth: inherit
        settings:
          encodeUrl: true
          timeout: 0
          followRedirects: true
          maxRedirects: 5
  - info:
      name: Projects
      type: folder
      seq: 3
    request:
      auth: inherit
    items:
      - info:
          name: GET Buscar por Id
          type: http
          seq: 3
        http:
          method: GET
          url: http://localhost:3000/projects/1
          headers:
            - name: Authorization
              value: Bearer {{token}}
          auth: inherit
        settings:
          encodeUrl: true
          timeout: 0
          followRedirects: true
          maxRedirects: 5
      - info:
          name: GET Listar Projetos
          type: http
          seq: 1
        http:
          method: GET
          url: http://localhost:3000/projects
          headers:
            - name: Authorization
              value: Bearer {{token}}
          auth: inherit
        settings:
          encodeUrl: true
          timeout: 0
          followRedirects: true
          maxRedirects: 5
      - info:
          name: GET Buscar com Tasks (include)
          type: http
          seq: 1
        http:
          method: GET
          url: http://localhost:3000/projects/2/tasks
          headers:
            - name: Authorization
              value: Bearer {{token}}
          auth: inherit
        settings:
          encodeUrl: true
          timeout: 0
          followRedirects: true
          maxRedirects: 5
      - info:
          name: POST Criar Projetos
          type: http
          seq: 1
        http:
          method: POST
          url: http://localhost:3000/projects
          headers:
            - name: Authorization
              value: Bearer {{token}}
          body:
            type: json
            data: |-
              {
                "name": "API Project",
                "status": "active",
                "description": "teste"
              }
          auth: inherit
        settings:
          encodeUrl: true
          timeout: 0
          followRedirects: true
          maxRedirects: 5
      - info:
          name: PUT Atualizar Projetos
          type: http
          seq: 1
        http:
          method: PUT
          url: http://localhost:3000/projects/6
          headers:
            - name: Authorization
              value: Bearer {{token}}
          body:
            type: json
            data: |-
              {
                "name": "API Project",
                "status": "active",
                "description": "teste"
              }
          auth: inherit
        settings:
          encodeUrl: true
          timeout: 0
          followRedirects: true
          maxRedirects: 5
      - info:
          name: DELETE Deletar Projetos
          type: http
          seq: 1
        http:
          method: DELETE
          url: http://localhost:3000/projects/1
          headers:
            - name: Authorization
              value: Bearer {{token}}
          auth: inherit
        settings:
          encodeUrl: true
          timeout: 0
          followRedirects: true
          maxRedirects: 5
  - info:
      name: Categories
      type: folder
      seq: 4
    request:
      auth: inherit
    items:
      - info:
          name: GET Listar Categorias
          type: http
          seq: 5
        http:
          method: GET
          url: http://localhost:3000/categories/
          headers:
            - name: Authorization
              value: Bearer {{token}}
          auth: inherit
        settings:
          encodeUrl: true
          timeout: 0
          followRedirects: true
          maxRedirects: 5
      - info:
          name: GET Buscar por Id
          type: http
          seq: 4
        http:
          method: GET
          url: http://localhost:3000/categories/1
          headers:
            - name: Authorization
              value: Bearer {{token}}
          auth: inherit
        settings:
          encodeUrl: true
          timeout: 0
          followRedirects: true
          maxRedirects: 5
      - info:
          name: Get Buscar com Tasks (Include)
          type: http
          seq: 2
        http:
          method: GET
          url: http://localhost:3000/categories/2/tasks
          headers:
            - name: Authorization
              value: Bearer {{token}}
          auth: inherit
        settings:
          encodeUrl: true
          timeout: 0
          followRedirects: true
          maxRedirects: 5
      - info:
          name: POST Criar Categoria
          type: http
          seq: 3
        http:
          method: POST
          url: http://localhost:3000/categories
          headers:
            - name: Authorization
              value: Bearer {{token}}
          body:
            type: json
            data: |-
              {
                "name": "Backend",
                "color": "#7c8cff"
              }
          auth: inherit
        settings:
          encodeUrl: true
          timeout: 0
          followRedirects: true
          maxRedirects: 5
      - info:
          name: PUT Atualizar Categoria
          type: http
          seq: 1
        http:
          method: PUT
          url: http://localhost:3000/categories/1
          headers:
            - name: Authorization
              value: Bearer {{token}}
          auth: inherit
        settings:
          encodeUrl: true
          timeout: 0
          followRedirects: true
          maxRedirects: 5
      - info:
          name: DELETE Deletar Categoria
          type: http
          seq: 1
        http:
          method: DELETE
          url: http://localhost:3000/categories/1
          headers:
            - name: Authorization
              value: Bearer {{token}}
          params:
            - name: ''
              value: ''
              type: query
          auth: inherit
        settings:
          encodeUrl: true
          timeout: 0
          followRedirects: true
          maxRedirects: 5
  - info:
      name: Dashboard
      type: folder
      seq: 5
    request:
      auth: inherit
    items:
      - info:
          name: GET Dashboard
          type: http
          seq: 1
        http:
          method: GET
          url: http://localhost:3000/dashboard
          headers:
            - name: Authorization
              value: Bearer {{token}}
          auth: inherit
        settings:
          encodeUrl: true
          timeout: 0
          followRedirects: true
          maxRedirects: 5
bundled: true
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
    exportedAt: '2026-07-02T15:36:49.344Z'
    exportedUsing: Bruno
